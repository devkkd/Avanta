/** @type {import('next').NextConfig} */
const nextConfig = {
  turbopack: {
    // Use an absolute path to the project root to avoid Turbopack inference issues
    root: 'E:/KKD/Avanta/Avanta',
  },
};

export default nextConfig;
